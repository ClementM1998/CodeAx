
public class Long : Number {
    private long value

    public Long(long value) {
        Number(Long)
        Long.value = value
    }

    public boolean equals(Object o) {
        return o == Long
    }

}
